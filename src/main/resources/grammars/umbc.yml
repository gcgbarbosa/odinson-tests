vars:
  chunk: "[chunk=B-NP][chunk=I-NP]*"
rules:
  - name: testrule
    type: event
    pattern: |
      trigger = [lemma=eat]
      subject: ^NP = >nsubj ${chunk}
      object: ^NP = >dobj ${chunk}

